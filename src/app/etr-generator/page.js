import ETRGenerator from "../../components/ETRGenerator";

export default function ETRGeneratorPage() {
	return <ETRGenerator />;
}

// Optional: Add metadata for the page
export const metadata = {
	title: "eTR產生器 - 豪神APP",
	description: "e-TAHI Report Generator",
};
